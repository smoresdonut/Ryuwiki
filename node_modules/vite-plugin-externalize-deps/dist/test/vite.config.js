import { defineConfig } from 'vite';
import { externalizeDeps } from '../dist';
export default defineConfig({
    build: {
        lib: {
            entry: './test/entry.ts',
            formats: ['es'],
        },
        outDir: './test/dist',
    },
    plugins: [
        externalizeDeps({
        // useFile: './test/package.json',
        }),
    ],
});
//# sourceMappingURL=vite.config.js.map